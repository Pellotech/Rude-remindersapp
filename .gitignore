node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
*.js
*.mjs

# JetBrains/Android Studio local IDE metadata — machine-specific, not used
# by the actual Gradle build, shouldn't be shared or versioned.
.idea/
android/.idea/
project-code.zip
