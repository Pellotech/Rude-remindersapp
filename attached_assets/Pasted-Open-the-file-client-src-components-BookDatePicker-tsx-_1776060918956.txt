Open the file: client/src/components/BookDatePicker.tsx

I need 3 specific visual fixes to the BookDatePicker. Do NOT change any logic, 
callbacks, state, or functionality. Only change the CSS/style values listed below.

---

FIX 1 — Reduce overall card/book height by 1/3

Find the outer wrapper or card container that sets the height of the book 
component. The current height is approximately 220px in closed state. 
Reduce it to approximately 145px. Also find any min-height or fixed height 
values on the book itself (open or closed state) and reduce them proportionally 
by 1/3. The goal is a more compact book — closer to the original date picker size.

---

FIX 2 — Remove rounded corners from the open book state

When the book is open (currentIdx >= 1), the outer wrapper or book container 
has border-radius applied. Find that border-radius and set it to 0 (no rounding). 
The open book should have sharp, square, boxed edges — not rounded. 
The closed state can keep its slight rounding (border-radius: 5px 7px 7px 5px) 
but the open state must be fully squared off.

---

FIX 3 — Reduce the right-side fan page layers (transcending depth area)

The right page section has fan layers positioned from the right edge inward 
(right:0, right:4px, right:8px... up to right:24px or right:28px). 
These layers are eating into the cream writing area on the right page.
Reduce each layer's width AND their right-offset positions by 2/3.

Specifically:
- If layers currently go from right:0 to right:24px, compress them so the 
  outermost layer stays at right:0 but the total spread only reaches right:8px
- Reduce the overall width of the right page fan section container by 2/3
- The cream area (#EFE5D0) on the right page should now take up much more 
  horizontal space — it should be the dominant element on the right page

The left page fan layers are fine — do NOT change the left side at all.

---

After making all 3 changes, confirm:
- No logic or state was changed
- Only style/CSS values were modified
- The book still opens and closes correctly
- The controls row (arrow buttons, Select this day, days pill) still appears below