Several improvements needed for the Manage tab and analytics.

Files to read FIRST before changing anything:
- client/src/components/RemindersList.tsx
- client/src/components/RudyWidget.tsx
- client/src/pages/analytics.tsx (or wherever My Journey 
  graph lives)
- server/routes.ts
- shared/schema.ts

DO NOT touch any other files not listed above.

═══════════════════════════════════════════
CHANGE 1 — Green and red rings around 
Did it / Didn't do it emojis
═══════════════════════════════════════════

In RemindersList.tsx find the 😊 and 😔 emoji buttons 
on past reminder cards.

Add a colored ring around each emoji button:

Did it emoji 😊:
- Border: 2.5px solid #22C55E (green)
- Border radius: 50% (full circle)
- Padding: 3px
- The green ring sits tightly around the emoji

Didn't do it emoji 😔:
- Border: 2.5px solid #C53B3B (red)
- Border radius: 50% (full circle)
- Padding: 3px
- The red ring sits tightly around the emoji

These rings are always visible on all past reminder 
cards so users immediately see what each button does.

═══════════════════════════════════════════
CHANGE 2 — Toast confirmation when 
emoji buttons are tapped
═══════════════════════════════════════════

In RemindersList.tsx add a small inline tooltip-style 
toast that appears right next to the tapped emoji button.

When user taps the Did it 😊 button:
- Show toast with text: "It's done ✅"
- Background: #1a1a1a (dark)
- Text: white
- Font size: 12px
- Border radius: 8px
- Padding: 6px 12px
- Appears directly above or next to the 😊 button
- Auto-dismisses after 2000ms
- No full screen overlay, no modal, just a tiny label

When user taps the Didn't do it 😔 button:
- Show toast with text: "I slacked 😬"
- Same styling as above
- Appears directly above or next to the 😔 button
- Auto-dismisses after 2000ms

Implementation:
Use a state to track which reminder id and which 
button is showing a toast:
const [emojiToast, setEmojiToast] = useState<{
  reminderId: number;
  type: 'did' | 'didnt';
} | null>(null);

When button tapped:
  setEmojiToast({ reminderId: reminder.id, type: 'did' });
  setTimeout(() => setEmojiToast(null), 2000);

In the JSX for each reminder card render the toast 
using position: relative on the emoji button wrapper
and position: absolute on the toast:
  position: absolute
  bottom: 100% (appears above the button)
  left: 50%
  transform: translateX(-50%)
  white-space: nowrap
  z-index: 10

Only show the toast for the specific reminder card 
that was tapped — not globally.

═══════════════════════════════════════════
CHANGE 3 — Instructional banner that 
shows then fades out every time user 
opens the Manage tab
═══════════════════════════════════════════

In RemindersList.tsx add an instructional banner 
that appears at the top of the Past reminders list
every single time the Manage tab is opened.

Behaviour:
- Fades IN immediately when Manage tab opens
- Stays visible for 3 seconds
- Fades OUT smoothly over 500ms
- Repeats every time the tab is opened — not just once

Text: "Tap 😊 or 😔 on each reminder to log what happened"

Style:
- Background: #FDF3E3 (light cream)
- Border: 1px solid #C9A063 (gold)
- Border radius: 10px
- Padding: 8px 14px
- Font size: 12px
- Color: #555
- Italic
- Centered text
- CSS transition: opacity 0.5s ease for fade in/out
- Margin bottom: 10px before the first reminder card

Implementation:
const [showHint, setShowHint] = useState(false);
const [hintFading, setHintFading] = useState(false);

When Manage tab becomes active:
  setShowHint(true);
  setHintFading(false);
  setTimeout(() => setHintFading(true), 3000);
  setTimeout(() => setShowHint(false), 3500);

JSX:
{showHint && (
  <div style={{
    opacity: hintFading ? 0 : 1,
    transition: 'opacity 0.5s ease',
  }}>
    Tap 😊 or 😔 on each reminder to log what happened
  </div>
)}

═══════════════════════════════════════════
CHANGE 4 — One-time tooltip from Rudy 
below the instructional banner
═══════════════════════════════════════════

Below the fading banner add a one-time tooltip that 
only ever shows ONCE — the very first time the user 
opens the Manage tab. Never shows again after dismissed.

Use localStorage to track if it has been seen:
  localStorage key: 'manage_tooltip_seen'

When manage_tooltip_seen is not set show the tooltip.
When user taps X or after 6 seconds:
  localStorage.setItem('manage_tooltip_seen', 'true')
  hide it permanently.

Tooltip content:
- Small Rudy image: /rudy/Rudy_leaning_2_transparent.png
  size 36x36px, mix-blend-mode: multiply
- Text next to Rudy: "First time here? The emoji buttons 
  track your habits. Green means done. Red means missed. 
  Your graph updates automatically."
- A small X dismiss button top right

Tooltip style:
- Background: white
- Border: 2px solid #C9A063 (gold)
- Border radius: 12px
- Padding: 10px 12px
- Font size: 11px
- Color: #333
- Display: flex, align-items: center, gap: 8px
- Margin bottom: 10px

═══════════════════════════════════════════
CHANGE 5 — Rudy reacts on Manage tab
═══════════════════════════════════════════

In RudyWidget.tsx update manageLoad lines in RUDY_LINES:

  manageLoad: {
    rude: [
      "Here's your history. Tell the truth. I'm watching.",
      "Manage tab. Where excuses go to die.",
      "All your reminders. All your choices. Own them.",
      "Tap the green or red emoji. Don't pretend you forgot.",
      "Those emojis aren't decorations. Use them.",
      // ADD MORE RUDE HERE
    ],
    positive: [
      "Your history is right here. Own every entry.",
      "Tap 😊 if you did it, 😔 if you didn't. Simple.",
      "Every entry matters. Tap the emoji to log it.",
      // ADD MORE POSITIVE HERE
    ],
  },

Keep the existing manage_load nudgeEvent wiring in 
home-premium.tsx — it already fires when Manage tab 
opens so no changes needed there.

═══════════════════════════════════════════
CHANGE 6 — Confirm analytics collects 
data from manage tab actions
═══════════════════════════════════════════

In server/routes.ts confirm that when a user taps 
Did it or Didn't do it the action is logged to the 
reminder_events table with:
- userId
- reminderId
- action: 'completed' or 'missed'
- scheduledFor
- createdAt

If this logging is missing or broken add it.

In analytics.tsx confirm the My Journey graph reads 
from reminder_events table NOT from the reminders table.
If it is reading from the wrong table fix it to read 
from reminder_events so deleted reminders do not 
affect the graph.

If both are already correct leave them untouched and 
just confirm in the response that they are working.

═══════════════════════════════════════════
FINAL RULES
═══════════════════════════════════════════

- Do NOT change the analytics graph visual styling
- Do NOT change button colors or functionality
- Do NOT change home-free.tsx
- Do NOT touch RudyAnimation.tsx
- Show changed sections of all files before applying
- Make changes safe and incremental