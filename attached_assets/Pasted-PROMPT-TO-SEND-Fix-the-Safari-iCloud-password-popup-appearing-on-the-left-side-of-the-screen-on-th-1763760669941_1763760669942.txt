PROMPT TO SEND:

Fix the Safari/iCloud password popup appearing on the left side of the screen on the login/register page.
The autocomplete fixes you applied were not enough.
I need the complete fix implemented properly.
Please do ALL of the following:

1. Completely disable browser autofill for ALL email + password inputs

Update every login/register <input> field with:

autoComplete="off"
autoCorrect="off"
spellCheck={false}
data-lpignore="true"
data-form-type="other"
formNoValidate


Safari ignores autocomplete="off" alone.
You MUST also include:

data-lpignore="true" (LastPass/iCloud ignore)

data-form-type="other"

formNoValidate
These stop Apple Keychain from showing.

2. Wrap the entire login form in a fake/hidden form to trick Safari

Add this above the visible form:

<form style="display:none">
  <input type="text" name="fakeusernameremembered"/>
  <input type="password" name="fakepasswordremembered"/>
</form>


This is a known workaround that forces Safari to autofill the hidden form instead of the real one.

3. Add autocomplete="new-password" to register password fields

This stops iCloud from offering stored passwords during signup.

Example:

autoComplete="new-password"

4. Ensure the visible login form has a unique/random form name & no browser hints

Modify the <form> tag to:

<form
  name="form-do-not-autofill"
  autoComplete="off"
  autoCapitalize="none"
  autoCorrect="off"
  data-lpignore="true"
  data-form-type="other"
>

5. Add CSS to disable Apple’s password icon from appearing

Add:

input::-webkit-credentials-auto-fill-button {
  visibility: hidden;
  display: none !important;
  pointer-events: none;
  opacity: 0;
}

6. Confirm fixes applied in BOTH components

client/src/pages/login.tsx

client/src/components/EmailAuthForm.tsx

any other place with <input type="email"> or <input type="password">

The goal:
Remove ALL iCloud/Safari autofill popups, password suggestions, and account bubbles. The login page must look clean with no automatic overlays popping up.

Apply the change, restart the app, and confirm autofill no longer appears.