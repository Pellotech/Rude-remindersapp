FIX 2 — Move Go Premium toast to appear 
near the locked button
═══════════════════════════════════════════

In home-free.tsx find the Photo and Quotes locked buttons.

When tapped show a small toast that appears directly above 
or below the tapped button — NOT fixed at the bottom of screen.

Use a small inline tooltip-style popup:
- Background: #1a1a1a (dark)
- Text: white, font size 12px
- Border radius: 8px
- Padding: 6px 12px
- Text: "🔒 Go Premium"
- Appears right next to the button that was tapped
- Auto-dismisses after 2000ms
- No full screen overlay, no modal, just a tiny label

Position it using relative/absolute positioning near 
the button — above the button is preferred so it does 
not get cut off.

═══════════════════════════════════════════
FIX 3 — Replace the big Go Premium button 
in RudyWidget area on home-free with a 
talk bubble and smaller button
═══════════════════════════════════════════

In home-free.tsx find where RudyWidget is rendered.
Currently it has showPremiumButton={true} which shows 
a big red Go Premium button in the reaction bubble slot.

In RudyWidget.tsx update the premium button slot to show 
this instead when showPremiumButton is true:

Replace the big button with this two-part layout 
inside the reaction bubble slot:

PART A — Small talk bubble above:
- Style it like the slogan bubble — same rounded shape,
  same black border, same white background
- BUT text color: #C53B3B (red) and font style: italic
- Text: "Find out what Rudy really has to say about you.."
- Small triangle pointer on LEFT pointing toward Rudy
- This sits where the reaction bubble normally sits

PART B — Small Go Premium button below the talk bubble:
- Background: #C53B3B (red)
- Text: "🔥 Go Premium"
- Text color: white
- Font size: 11px
- Font weight: bold
- Border radius: 20px
- Padding: 5px 14px
- Width: fit-content, centered
- Calls onPremiumPress when tapped

If both parts do not fit comfortably in the current 
widget height increase the widget min-height to 140px.

The slogan bubble at the top stays completely unchanged —
still rotates every 20 seconds, same styling.
Rudy image stays unchanged.

Only the bottom slot changes — talk bubble + small button
instead of the big red button.

═══════════════════════════════════════════
FINAL RULES
═══════════════════════════════════════════

- Do NOT change home-premium.tsx
- Do NOT change RUDY_LINES or the reaction system
- Do NOT change the slogan bubble
- Do NOT touch RudyAnimation.tsx
- Show changed sections before applying
- Make changes safe and incremental