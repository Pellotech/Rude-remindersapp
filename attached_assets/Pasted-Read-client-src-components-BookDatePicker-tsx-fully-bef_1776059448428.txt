Read client/src/components/BookDatePicker.tsx fully before 
writing a single line. Then rebuild it completely to match 
the design spec below. Keep ALL existing props, state logic, 
onScheduleChange firing logic, hour picker, and minute picker 
exactly as they are — only change the visual structure.

━━━━━━━━━━━━━━━━━━━━━━━━━━
BOOK VISUAL STRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━

The book has TWO states: CLOSED and OPEN.
Both states live inside a wrapper div with:
- background: #FDF3E3
- border: 1.5px solid #C9A063
- border-radius: 14px
- padding: 20px
- display: flex, align-items: center, justify-content: center

━━━━━━━━━━━━━━━━━━━━━━━━━━
CLOSED STATE (currentIdx === 0, cover not yet flipped)
━━━━━━━━━━━━━━━━━━━━━━━━━━

Book sits centered, width: 35% of container, height: 220px.
box-shadow: 5px 5px 18px rgba(0,0,0,0.32), -1px 0 4px rgba(0,0,0,0.1)
border-radius: 5px 7px 7px 5px (left rounded for spine)
display: flex, overflow: hidden

SPINE (left side of closed book):
- width: 14px, background: #3d1a08
- border-radius: 5px 0 0 5px
- ::after pseudo: right 0, width 3px, 
  background rgba(255,255,255,0.06)

COVER (right side, flex: 1):
- background: #4a1e0a
- border-radius: 0 7px 7px 0
- position relative, overflow hidden
- flex column, align center, justify center, gap 5px
- padding: 10px
- ::before pseudo: left 0, width 6px, 
  background rgba(0,0,0,0.15) — inner spine shadow

COVER CONTENT (centered, no ring around logo):
- Logo: import from '@assets/translusant_logo2_1767108484844.png'
  width 32px, height 32px, object-fit contain
  (no circle border around it — logo floats free)
- Divider: width 26px, height 1px, 
  background rgba(201,160,99,0.3)
- Title text "RUDE REMINDERS": 
  color #C9A063, font-size 6.5px, letter-spacing 0.12em,
  font-weight 500, text-align center, line-height 1.7
- Sub text "your week ahead":
  color rgba(201,160,99,0.5), font-size 5.5px,
  letter-spacing 0.07em

RIGHT PAGE EDGE (on closed book — shows pages):
- position absolute, right 0, top 0, bottom 0, width 5px
- display flex, flex-direction column, overflow hidden
- border-radius 0 7px 7px 0
- Each line: flex 1, background #F5EFE6, 
  border-bottom 1px solid rgba(0,0,0,0.06)
- Render 12 of these lines

Below the closed book, centered hint text:
- "swipe or press → to open"
- font-size 11px, color muted, margin-top 8px

━━━━━━━━━━━━━━━━━━━━━━━━━━
OPEN STATE (currentIdx >= 1)
━━━━━━━━━━━━━━━━━━━━━━━━━━

When open, book fills the full container width.
Outer wrapper: position relative, width 100%, padding 6px 0

TOP EDGE (position absolute, top 0, left 13px, right 13px):
- height: 6px, background: #3d1a08
- border-radius: 3px 3px 0 0

BOTTOM EDGE (position absolute, bottom 0, left 13px, right 13px):
- height: 6px, background: #3d1a08  
- border-radius: 0 0 3px 3px

OPEN BOOK (width 100%, height 210px):
- display flex, border-radius 4px, overflow hidden
- box-shadow: 0 4px 22px rgba(0,0,0,0.3)

LEFT OUTER COVER:
- width: 13px, background: #3d1a08
- border-radius: 4px 0 0 4px

LEFT PAGE SECTION (flex 1):
- position relative, background #c8a870
- display flex, align center, justify center
- Stacked fan pages (position absolute, inset 0):
  Each pf div: position absolute, top 0, bottom 0
  Layer 1 (left 0):   background #a88040
  Layer 2 (left 4px): background #b28848
  Layer 3 (left 8px): background #ba9052
  Layer 4 (left 12px): background #c29860
  Layer 5 (left 16px): background #caa26c
  Layer 6 (left 20px): background #d0aa78
  Layer 7 (left 24px): background #d6b484
  Layer 8 (left 28px): background #dcbc90
- Right shadow overlay: position absolute, right 0, 
  top 0, bottom 0, width 22px,
  background linear-gradient(to right, transparent, 
  rgba(0,0,0,0.22)), zIndex 5
- Ghost number + day name (previous page):
  position relative, zIndex 6, text-align center
  Number: font-size 32px, font-weight 500, 
    color rgba(50,20,5,0.2)
  Day: font-size 10px, color rgba(50,20,5,0.15), 
    margin-top 2px

CENTER SPINE:
- width: 20px, background: #3d1a08
- zIndex 10, box-shadow: 2px 0 8px rgba(0,0,0,0.35),
  -2px 0 8px rgba(0,0,0,0.25)
- display flex, flex-direction column, 
  align center, justify center, gap 6px
- position relative
- ::before: left 0, width 5px, 
  background linear-gradient(to right, rgba(0,0,0,0.3), transparent)
- ::after: right 0, width 5px,
  background linear-gradient(to left, rgba(0,0,0,0.3), transparent)
- 3 gold dots (5px circles, background #C9A063) 
  with 2 lines between them (width 1px, height 12px, 
  background rgba(255,255,255,0.15))
- All dots and lines: position relative, zIndex 2

RIGHT PAGE SECTION (flex 1):
- position relative, overflow hidden
- display flex, flex-direction column, 
  align center, justify center, gap 4px

RIGHT PAGE FAN LAYERS (position absolute, inset 0):
Each rf div: position absolute, top 0, bottom 0
  Layer 1 (left 0, right 0):   background #dcbc90
  Layer 2 (left 0, right 4px): background #d6b484
  Layer 3 (left 0, right 8px): background #d0aa78
  Layer 4 (left 0, right 12px): background #caa26c
  Layer 5 (left 0, right 16px): background #c29860
  Layer 6 (left 0, right 20px): background #ba9052
  Layer 7 (left 0, right 24px): background #EFE5D0

TRANSCENDING DEPTH OVERLAYS (all pointer-events none):
- Left shadow: position absolute, left 0, top 0, bottom 0,
  width 20px, background linear-gradient(to right,
  rgba(60,25,5,0.22), transparent), zIndex 4
- Right shadow: position absolute, right 0, top 0, bottom 0,
  width 20px, background linear-gradient(to left,
  rgba(60,25,5,0.2), transparent), zIndex 4
- Top shadow: position absolute, top 0, left 0, right 0,
  height 18px, background linear-gradient(to bottom,
  rgba(60,25,5,0.18), transparent), zIndex 4
- Bottom shadow: position absolute, bottom 0, left 0, right 0,
  height 18px, background linear-gradient(to top,
  rgba(60,25,5,0.18), transparent), zIndex 4
- Center glow: position absolute, inset 0,
  background radial-gradient(ellipse at 50% 50%,
  rgba(255,248,230,0.45) 0%, transparent 68%), zIndex 3

RULED LINES (position absolute, inset 0, zIndex 5, 
pointer-events none):
- display flex, flex-direction column, 
  justify-content flex-end, padding 8px 12px, gap 10px
- 5 lines: height 1px, 
  background rgba(150,100,50,0.13)

TODAY BADGE (position absolute, top 10px, right 30px, zIndex 8):
- background: #5c2d0e, color: #F5EFE6
- font-size 9px, font-weight 500
- padding: 2px 6px, border-radius: 3px
- Only show on page index 1 (today)

DATE NUMBER (position relative, zIndex 6):
- font-size: 54px, font-weight: 500, line-height: 1
- Unselected: color #1a1005
- Selected: color #C53B3B
- transition: color 0.2s

DAY NAME (position relative, zIndex 6):
- font-size: 14px
- Unselected: color #3d2010
- Selected: color rgba(197,59,59,0.78)
- transition: color 0.2s

MONTH NAME (position relative, zIndex 6):
- font-size: 12px, font-weight: 500, color: #C9A063

RUDY SCRIBBLE SVG (position absolute, bottom 10px, 
right 30px, width 36px, height 36px, zIndex 8,
pointer-events none):
- opacity 0 when not selected
- opacity 1 when selected, triggers draw animation
- viewBox="0 0 46 46" fill="none"
- Face circle: cx=23 cy=21 r=12, stroke #C53B3B,
  stroke-width 1.8, stroke-linecap round,
  stroke-dasharray 300, stroke-dashoffset 300,
  animation: scribble 0.65s ease forwards
- Left eye: circle cx=19 cy=18 r=1.8 fill #C53B3B
- Right eye: circle cx=27 cy=18 r=1.8 fill #C53B3B
- Smile: path d="M18 25 Q23 30 28 25", stroke #C53B3B,
  stroke-width 1.8, stroke-linecap round,
  stroke-dasharray 300, stroke-dashoffset 300,
  animation: scribble 0.65s ease forwards 0.2s
- Left brow: path d="M16 13 Q18 11 20 13",
  stroke #C9A063, stroke-width 1.4,
  stroke-dasharray 100, stroke-dashoffset 100,
  animation: scribble 0.4s ease forwards 0.4s
- Right brow: path d="M26 13 Q28 11 30 13",
  stroke #C9A063, stroke-width 1.4,
  stroke-dasharray 100, stroke-dashoffset 100,
  animation: scribble 0.4s ease forwards 0.5s
@keyframes scribble { to { stroke-dashoffset: 0 } }
On select: remove animation class, force reflow 
(offsetWidth), re-add animation class

RIGHT OUTER COVER:
- width: 13px, background: #3d1a08
- border-radius: 0 4px 4px 0
- No lines, no tan, just solid dark brown

━━━━━━━━━━━━━━━━━━━━━━━━━━
TRANSITION BETWEEN CLOSED AND OPEN
━━━━━━━━━━━━━━━━━━━━━━━━━━

When currentIdx moves from 0 to 1 (cover opens):
- Closed book animates: 
  transform scale(1) → scale(0.95) opacity 1 → 0
  over 200ms, then switch to open state
- Open book animates in:
  opacity 0 → 1, transform translateY(6px) → translateY(0)
  over 300ms ease

Use a React state flag `isTransitioning` to handle this.
Use CSS transition classes, not a library.

━━━━━━━━━━━━━━━━━━━━━━━━━━
CONTROLS ROW (below book, one line, gap 8px)
━━━━━━━━━━━━━━━━━━━━━━━━━━

All items height 36px:

Left arrow button:
- background #FDF3E3, border 1.5px solid #C9A063
- border-radius 20px, width 42px, color #C9A063
- hover: background #C9A063, color white
- disabled + opacity 0.25 when currentIdx === 0

Centre button (flex 1):
- border-radius 20px, font-size 13px, font-weight 500
- currentIdx === 0: text "Open cover",
  background #C9A063, color white, onClick → goTo(1)
- currentIdx > 0, page unselected: "Select this day",
  background #C9A063, color white
- currentIdx > 0, page selected: "Deselect",
  background white, color #C53B3B,
  border 1.5px solid #C53B3B

Right arrow button: same as left,
disabled when currentIdx === 7

Counter pill (flex-shrink 0):
- border 1.5px solid #C9A063, border-radius 20px
- min-width 46px, height 36px, padding 0 10px
- Number: font-size 15px, font-weight 500
  color #C53B3B when > 0, color #C9A063 when 0
- "days" label: font-size 9px, color #C9A063

━━━━━━━━━━━━━━━━━━━━━━━━━━
META ROW (below controls, space-between)
━━━━━━━━━━━━━━━━━━━━━━━━━━

Left — 8 progress dots:
- Cover dot (i=0): background #6B3410
- Selected date dots: background #C53B3B
- Current page dot: background #C9A063, 
  width 14px, border-radius 3px
- Others: background rgba(0,0,0,0.15), 6x6px circles
- transition all 0.25s

Right — selected date chips:
- background #C53B3B, color white
- font-size 10px, border-radius 8px, padding 2px 7px
- Format: "Mon 13", animate in scale 0.7→1

━━━━━━━━━━━━━━━━━━━━━━━━━━
HOUR + MINUTE PICKERS
━━━━━━━━━━━━━━━━━━━━━━━━━━

Keep these EXACTLY as they currently are.
Do not change any logic, styling, or behavior.

━━━━━━━━━━━━━━━━━━━━━━━━━━
SWIPE HANDLING
━━━━━━━━━━━━━━━━━━━━━━━━━━

On the open book wrapper:
- touchstart: record clientX
- touchend: if |dx| > 35px go forward or back
- mousedown/mouseup: same for desktop
- isAnimating ref blocks rapid taps (550ms lock)

━━━━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL — DO NOT CHANGE:
━━━━━━━━━━━━━━━━━━━━━━━━━━
- onScheduleChange firing logic — untouched
- onDateEventFired — untouched
- Hour picker logic and styling — untouched
- Minute picker logic and styling — untouched
- All props interface — untouched
- ReminderForm.tsx — do not touch at all
- Show me the full updated BookDatePicker.tsx 
  before applying